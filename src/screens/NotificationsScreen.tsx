import { Pressable,StyleSheet,Text,View } from "react-native";import { NativeStackScreenProps } from "@react-navigation/native-stack";import { RootStackParamList } from "../navigation/types";import { Screen } from "../components/Screen";import { Header } from "../components/ui";import { notifications } from "../mocks/moduleData";import { theme } from "../theme/theme";
export function NotificationsScreen({navigation}:NativeStackScreenProps<RootStackParamList,"Notifications">){return <Screen><Header title="Notifications" subtitle="Alerts that navigate directly to the relevant mobile module." />{notifications.map(n=><Pressable style={s.row} key={n.id} onPress={()=>navigation.navigate("Module",{moduleKey:n.moduleKey})}><View style={s.dot}/><View style={{flex:1}}><Text style={s.title}>{n.title}</Text><Text style={s.body}>{n.body}</Text></View></Pressable>)}</Screen>}
const s=StyleSheet.create({row:{flexDirection:"row",gap:11,backgroundColor:"white",padding:14,borderRadius:14,borderWidth:1,borderColor:theme.colors.line,marginBottom:9},dot:{width:9,height:9,borderRadius:9,backgroundColor:theme.colors.primary,marginTop:4},title:{fontWeight:"800",color:theme.colors.text},body:{fontSize:11,color:theme.colors.muted,lineHeight:17,marginTop:4}})
