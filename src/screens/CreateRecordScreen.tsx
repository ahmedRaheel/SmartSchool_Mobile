import { useState } from "react";import { Pressable,StyleSheet,Text,TextInput } from "react-native";import { NativeStackScreenProps } from "@react-navigation/native-stack";import { RootStackParamList } from "../navigation/types";import { Screen } from "../components/Screen";import { Header } from "../components/ui";import { modules } from "../mocks/moduleData";import { useApp } from "../app/AppProvider";import { theme } from "../theme/theme";
export function CreateRecordScreen({route,navigation}:NativeStackScreenProps<RootStackParamList,"CreateRecord">){const data=modules[route.params.moduleKey];const{notify}=useApp();const[title,setTitle]=useState("");const[detail,setDetail]=useState("");return <Screen><Header title={data.action} subtitle={`Create mock ${data.title.toLowerCase()} data`}/><Text style={s.label}>Title</Text><TextInput style={s.input} value={title} onChangeText={setTitle} placeholder="Enter title"/><Text style={s.label}>Detail</Text><TextInput style={s.input} value={detail} onChangeText={setDetail} placeholder="Enter detail"/><Pressable disabled={!title.trim()} style={[s.button,!title.trim()&&{opacity:.45}]} onPress={()=>{notify(`${title} created in mock workflow.`);navigation.goBack()}}><Text style={s.buttonText}>Save mock record</Text></Pressable></Screen>}
const s=StyleSheet.create({label:{fontSize:11,fontWeight:"700",color:theme.colors.muted,marginBottom:6},input:{height:48,backgroundColor:"white",borderWidth:1,borderColor:theme.colors.line,borderRadius:12,paddingHorizontal:12,marginBottom:14},button:{height:48,backgroundColor:theme.colors.primary,borderRadius:12,alignItems:"center",justifyContent:"center"},buttonText:{color:"white",fontWeight:"800"}})
